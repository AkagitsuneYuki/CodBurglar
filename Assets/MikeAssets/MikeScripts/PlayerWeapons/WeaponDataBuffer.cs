/*
 * This script is to be used to transfer the player's weapons between levels
 * 
 * This will work by storing the data in static variables
 * 
 */

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponDataBuffer : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
